name: Build & Release

on:
  push:
    tags:
      - "v*" # v1.0.0, v1.1.0 등 태그 push 시 빌드

jobs:
  build:
    strategy:
      fail-fast: false # Mac/Win 독립 빌드 — 한쪽 실패해도 나머지 계속
      matrix:
        include:
          - os: macos-latest
            platform: mac
          - os: windows-latest
            platform: win

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - uses: pnpm/action-setup@v4

      - name: Install dependencies
        run: pnpm install

      - name: Build Next.js
        run: pnpm build
        env:
          # Windows standalone 빌드 시 시스템 폴더 스캔 EPERM 방지
          USERPROFILE: ${{ runner.temp }}
          HOME: ${{ runner.temp }}

      - name: Build Electron
        run: pnpm tsc -p electron/tsconfig.json

      - name: Rebuild native modules for Electron
        run: pnpm electron-rebuild

      - name: Package (${{ matrix.platform }})
        run: pnpm electron-builder --${{ matrix.platform }} --publish always
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
